server:
  port: 8092
spring:
  datasource:
    url: jdbc:mysql://localhost/Allocation?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update